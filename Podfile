# Uncomment the next line to define a global platform for your project

platform :ios, '10.3'

target 'UserTracking' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftLint'
  pod 'RNCryptor', '~> 5.0'

  # Pods for UserTracking

  target 'UserTrackingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UserTrackingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
